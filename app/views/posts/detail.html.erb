<a href="/">back</a>

<p><i> Ecrit par : <%= @post.user.name %></i></p>
<p><i> Categorie : <%= @post.category.name %></i></p>
<h2>
  <span><%= @post.id %></span> <%= @post.title %>
</h2>
<p>
  <%= @post.content %>
</p>

<div>
  <% @post.tags.each do |tag| %>
    <div class="chip">
      <%= tag.name %>
    </div>
  <% end %>
</div>